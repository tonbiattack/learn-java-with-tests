# 認証が入った API のテスト

この章は **計画中** です。Spring Security を入れたときの最小限のテスト駆動を扱います。

## ねらい

- 認証前提の API 契約を `MockMvc` で固定する
- 権限による `403` と未認証の `401` を区別する
- 認証ロジックをビジネスロジックから切り離す

## 章で扱いたい内容

- `GET /profile`
- `POST /admin/...`
- `@WithMockUser`
- セキュリティ設定の最小構成

