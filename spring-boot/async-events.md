# 非同期イベントのテスト

この章は **計画中** です。Spring Boot のイベントや非同期実行を、テストしやすい形で扱います。

## ねらい

- `ApplicationEventPublisher` を直接アプリ層に漏らしすぎない
- 非同期化しても観測可能なテストを書く
- 副作用の境界を意識する

## 章で扱いたい内容

- ユーザー登録後に welcome mail イベントを publish する
- event listener のテスト
- 同期実行から非同期実行へ切り替える設計

